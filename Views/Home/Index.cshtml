@* @page *@
@using mvc_dotnet.DBContext;
@model IEnumerable<EmployeeSalarysModel>

@* @model HomeController *@
@{

  ViewData["Title"] = "Employees";
}


<style>
  body{
    background-color: black;
  }
  @* table {
    border-collapse: separate;
    border: 2px solid white;
    border-radius: 6px;
    color: white;
  } *@

  td,
  th {
    @* border-left: solid black 1px;
    border-top: solid black 1px; *@
    color: white;
  }

  th {

    color: white;
  }

  @* td:first-child,
  th:first-child {
    border-left: none;
    color: white;
  } *@



  #addBtn {
    text-align: center;
    display: block;
    width: 100%;
    height: 100%;
    background: black;
    padding: 10px;
    text-align: center;
    border-radius: 5px;
    color: white;
    font-weight: bold;
box-shadow: 0 0 40px rgb(207, 207, 207);
}

 
  
</style>




<h1 style="color: white;">@ViewData["Title"]</h1>
<br>
@* @using (Html.BeginForm("Index", "Home", FormMethod.Post))
{ *@

<table style="width:100%">
  <tr>
    <th>Id</th>
    <th>Name</th>
    <th>Post</th>
    <th>Salary</th>
    <th></th>
  </tr>
  @{
    @* Scaffold-DbContext "server=localhost;port=3306;user=root;password=Qwertyuiop1!;database=admin"
  MySql.Data.EntityFrameworkCore -OutputDir DataObjects -f *@
    @* Dictionary<string, double> name_hours = await Emp.Employees(); *@

    foreach (EmployeeSalarysModel f in Model)
    {
@* <form method="post" enctype="multipart/form-data" asp-controller="Home" asp-action="See_and_add"> *@

      <tr>
        <td>@f.Id.ToString()</td>
        <td>@f.Name.ToString()</td>
        <td>@f.Post.ToString()</td>
        <td>@f.Salary.ToString()</td>
       
      
      <td><a id="addBtn" asp-route-id="@f.Id.ToString()" asp-controller="Home" asp-action="See_and_add">Edit</a></td>

      
      </tr>
      @* </form> *@
    }

  }
</table>
@* } *@